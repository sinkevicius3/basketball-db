#include <stdio.h>
#include <stdbool.h> 
#include <string.h>
#include <stdlib.h>

#define MENU_OPTIONS 3
#define SQLCODE sqlca.sqlcode

void connect_db();
void disconnect_db();
void printMenuOptions();
void printPointerError(const char *msg);

int isIntegerInRange(const int rangeStart, const int rangeEnd, const int value);
int getValidatedInteger(const int rangeStart, const int rangeEnd, const char *inputMsg);
int getInput(const char *msg, char *buffer, int size);

void flushInput();

EXEC SQL INCLUDE sqlca.h;

EXEC SQL BEGIN DECLARE SECTION;
  const char *dbname = "basketball";
EXEC SQL END DECLARE SECTION;

void connect_db() {
  EXEC SQL CONNECT TO :dbname;

  if (SQLCODE != 0) {
    printf("Connection failed: %s\n", sqlca.sqlerrm.sqlerrmc);
    exit(1);
  }

  printf("Connected to database '%s'.\n", dbname);
}

void disconnect_db() {
  EXEC SQL DISCONNECT ALL;
}

void printMenuOptions() {
  printf("\n=== Basketball DB ===\n");
  printf("1) Add player.\n");
  printf("2) Show players.\n");
  printf("0) Exit.\n\n");
}

void printPointerError(const char *msg) {
  if (msg != NULL){
    printf ("Pointer error in: '%s'.\n\n", msg);
  } else {
    printf ("Pointer error in 'printPointerError'.\n\n");
  }
}

int isIntegerInRange(const int rangeStart, const int rangeEnd, const int value) {
  if ((value >= rangeStart) && (value <= rangeEnd)){
    return 1;
  }
  return 0;
}

int getValidatedInteger(const int rangeStart, const int rangeEnd, const char *inputMsg) {
  if (inputMsg != NULL){
    
    int number;
    char c;

    while (1){
      printf("%s", inputMsg);
      if (((scanf("%d%c", &number, &c) ==  2) && (c == '\n'))){

        if (isIntegerInRange(rangeStart, rangeEnd, number)){
          printf("Value %d was successfully registered.\n\n", number);
          break;
        }

        else{
          printf("Entered number is out of range! Please enter an integer value that is in the range [%d; %d].\n\n", rangeStart, rangeEnd);
        }
      } else {
          printf("Incorrect type of input! Please enter an integer value.\n\n");
          scanf("%*[^\n]%*c");
      }
    }

    return number;
  } else {
    printPointerError("getValidatedInteger");
    return -1;
  }
}

int getInput(const char *msg, char *buffer, int size) {
  if (msg == NULL || buffer == NULL) {
    printPointerError("getInput");
    return -1;
  }
  printf("%s", msg);
  fgets(buffer, size, stdin);
  if (buffer[strcspn(buffer, "\n")] == '\n') {
    buffer[strcspn(buffer, "\n")] = '\0';
  } else {
    flushInput();
  }
  return buffer[0] != '\0';
}

void flushInput() {
  int c;
  while ((c = getchar()) != '\n' && c != EOF);
}

void addPlayer() {
  EXEC SQL BEGIN DECLARE SECTION;
    char first_name[51];
    char last_name[51];
    char birth_date[11];
    char nationality[4];
    char position[2];
    int team_id;
    char start_date[11];
    char end_date[11];
    double value;
    int position_ind;
    int team_id_ind;
    int start_date_ind;
    int end_date_ind;
    int value_ind;
  EXEC SQL END DECLARE SECTION;

  char position_buf[2];
  char team_buf[12];
  char value_buf[20];

  EXEC SQL WHENEVER SQLERROR GOTO error;
  EXEC SQL WHENEVER NOT FOUND DO BREAK;

  getInput("Enter first name: ", first_name, sizeof(first_name));
  getInput("Enter last name: ", last_name, sizeof(last_name));
  getInput("Enter birth date (YYYY-MM-DD): ", birth_date, sizeof(birth_date));
  getInput("Enter nationality ('LTU', 'USA', ...): ", nationality, sizeof(nationality));

  strcpy(position, position_buf);
  if(getInput("Enter position ('G', 'F', 'C', or press Enter to skip): ", position_buf, sizeof(position_buf))) {
    position_ind = 0;
  } else {
    position_ind = -1;
  }

  printf("\n=== Available Teams ===\n");
  EXEC SQL DECLARE team_cursor CURSOR FOR
    SELECT id, name, city FROM team;
  EXEC SQL OPEN team_cursor;

  EXEC SQL BEGIN DECLARE SECTION;
    int t_id;
    char t_name[51];
    char t_city[51];
  EXEC SQL END DECLARE SECTION;

  while (1) {
    EXEC SQL FETCH team_cursor INTO :t_id, :t_name, :t_city;
    printf("%d) %s %s\n", t_id, t_city, t_name);
  }
  EXEC SQL CLOSE team_cursor;

  EXEC SQL WHENEVER NOT FOUND CONTINUE;

  if (getInput("\nEnter team ID (or press Enter to skip): ", team_buf, sizeof(team_buf))) {
    team_id = atoi(team_buf);
    team_id_ind = 0;

    getInput("Enter start date (YYYY-MM-DD): ", start_date, sizeof(start_date));
    getInput("Enter end date (YYYY-MM-DD): ", end_date, sizeof(end_date));
    getInput("Enter value: ", value_buf, sizeof(value_buf));

    value = atof(value_buf);
    start_date_ind = 0;
    end_date_ind = 0;
    value_ind = 0;
  } else {
    team_id_ind = -1;
    start_date_ind = -1;
    end_date_ind = -1;
    value_ind = -1;
  }

  EXEC SQL INSERT INTO player (first_name, last_name, birth_date, nationality, position, team, start_date, end_date, value)
  VALUES (:first_name, :last_name, :birth_date, :nationality, :position INDICATOR :position_ind,
    :team_id INDICATOR :team_id_ind, :start_date INDICATOR :start_date_ind,
    :end_date INDICATOR :end_date_ind, :value INDICATOR :value_ind);

  EXEC SQL COMMIT;
  printf("Player added successfully!\n");

  EXEC SQL WHENEVER SQLERROR CONTINUE;
  EXEC SQL WHENEVER NOT FOUND CONTINUE;
  return;

  error:
    printf("Error adding player: %s\n", sqlca.sqlerrm.sqlerrmc);
    EXEC SQL ROLLBACK;
    EXEC SQL WHENEVER SQLERROR CONTINUE;
    EXEC SQL WHENEVER NOT FOUND CONTINUE;
}

void showPlayers() {
  EXEC SQL BEGIN DECLARE SECTION;
    int p_id;
    char p_first_name[51];
    char p_last_name[51];
    char p_birth_date[11];
    char p_nationality[4];
    char p_position[2];
    int p_team;
    char p_start_date[11];
    char p_end_date[11];
    double p_value;
    int p_age;
    int p_contract_days_left;
    int p_team_ind;
    int p_position_ind;
    int p_start_date_ind;
    int p_end_date_ind;
    int p_value_ind;
    int p_contract_days_left_ind;
  EXEC SQL END DECLARE SECTION;

  EXEC SQL WHENEVER SQLERROR GOTO error;
  EXEC SQL WHENEVER NOT FOUND DO BREAK;

  printf("\n=== Players ===\n");

  EXEC SQL DECLARE player_cursor CURSOR FOR
    SELECT id, first_name, last_name, birth_date, nationality, position, team,
      start_date, end_date, value, age, contract_days_left
    FROM player_extended_info;

  EXEC SQL OPEN player_cursor;

  printf("|| %-4s || %-20s || %-20s || %-10s || %-3s || %-4s || %-4s || %-10s || %-10s || %-12s || %-4s || %-10s ||\n",
    "ID", "First name", "Last name", "Birth date", "Nat", "Pos", "Team",
    "Start date", "End date", "Value", "Age", "Days left");

  while (1) {
    EXEC SQL FETCH player_cursor INTO
      :p_id, :p_first_name, :p_last_name, :p_birth_date, :p_nationality,
      :p_position INDICATOR :p_position_ind,
      :p_team INDICATOR :p_team_ind,
      :p_start_date INDICATOR :p_start_date_ind,
      :p_end_date INDICATOR :p_end_date_ind,
      :p_value INDICATOR :p_value_ind,
      :p_age,
      :p_contract_days_left INDICATOR :p_contract_days_left_ind;

    printf("|| %-4d || %-20s || %-20s || %-10s || %-3s || %-4s || %-4d || %-10s || %-10s || %-12.2f || %-4d || %-10d ||\n",
      p_id, p_first_name, p_last_name, p_birth_date, p_nationality,
      p_position_ind == -1 ? "N/A" : p_position,
      p_team_ind == -1 ? 0 : p_team,
      p_start_date_ind == -1 ? "N/A" : p_start_date,
      p_end_date_ind == -1 ? "N/A" : p_end_date,
      p_value_ind == -1 ? 0.0 : p_value,
      p_age,
      p_contract_days_left_ind == -1 ? 0 : p_contract_days_left);
  }

  EXEC SQL CLOSE player_cursor;
  EXEC SQL WHENEVER SQLERROR CONTINUE;
  EXEC SQL WHENEVER NOT FOUND CONTINUE;
  return;

  error:
    printf("Database error: %s\n", sqlca.sqlerrm.sqlerrmc);
    EXEC SQL CLOSE player_cursor;
    EXEC SQL WHENEVER SQLERROR CONTINUE;
    EXEC SQL WHENEVER NOT FOUND CONTINUE;
}


int main (int argc, char**argv) {

  int option;

  connect_db();

  while (1){
    printMenuOptions();
    option = getValidatedInteger (0, (MENU_OPTIONS - 1), "Enter the option's number: ");

    switch (option){
      case 1: 
        addPlayer();
        break;
      case 2:
        showPlayers();
        break;
      case 0:
        disconnect_db();
        exit(0);
      default:
        printf("Option does not exist.\n");
        break;
    }
  }
  
  return 0;
}